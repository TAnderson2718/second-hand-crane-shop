interface PlaceholderConfig {
    projectName: string;
    templatePath: string;
    projectPath: string;
}
export declare function changeDefaultNameInTemplate({ projectName, templatePath, projectPath }: PlaceholderConfig): Promise<void>;
export {};
